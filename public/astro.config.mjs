import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://chircami.github.io',
  base: '/Portfolio_astro', // Solo si el repo NO es <usuario>.github.io
});